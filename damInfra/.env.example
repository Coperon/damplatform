# Copy to .env in this folder. Never commit the real .env.

# ---- PostgreSQL ----
POSTGRES_USER=dam
POSTGRES_PASSWORD=
POSTGRES_DB=dam

# ---- MinIO ----  (password MUST be at least 8 characters or MinIO won't start)
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
