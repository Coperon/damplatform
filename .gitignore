# ---- dependencies ----
node_modules/
.pnp
.pnp.*

# ---- build output ----
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# ---- environment / secrets ----
.env
.env.*
!.env.example

# ---- deployment ----
.vercel

# ---- database & media backups (never in git) ----
# dam_backup.sql contains live user rows and bcrypt password hashes.
# minio_backup.tar.gz is 155 MB, over GitHub's 100 MB file limit.
dam_backup.sql
*.tar.gz
minio_backup.tar.gz

# ---- logs ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---- editor / OS ----
.DS_Store
Thumbs.db
*.pem

# ---- machine-local agent settings (may contain credentials) ----
**/.claude/settings.local.json
